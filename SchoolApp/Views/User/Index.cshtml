@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@* ﻿@{
    ViewData["Title"] = "School App";
}

@using Microsoft.AspNetCore.Identity

@{
    if (User.IsInRole("Admin"))
    {
        Context.Response.Redirect(Url.Action("Dashboard", "Admin")!);
    }
    else if (User.IsInRole("Teacher"))
    {
        Context.Response.Redirect(Url.Action("Dashboard", "Teacher")!);
    }
    else if (User.IsInRole("Student"))
    {
        Context.Response.Redirect(Url.Action("Dashboard", "Student")!);
    }
    else
    {
        // fallback for users without a role
        <div class="text-center mt-5">
            <h4>No dashboard available for your role.</h4>
        </div>
    }
} *@
